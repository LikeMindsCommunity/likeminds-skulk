# Deployment spec file for GCP GKE skulk-beta deployment
# We have moved service to container based deployment with docker and AWS EKS
# File is deprecated and moved to deployment-deprecated directory

apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "subscription"
  namespace: "app-deploy"
  labels:
    app: "subscription"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "subscription"
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: "subscription"
    spec:
      containers:
      - name: "github-nateshr-likeminds-subscription-skulk-1"
        image: "asia.gcr.io/likeminds-nonprod-prj-24e1/github.com/nateshr/likeminds-subscription/skulk-beta:v3"
        resources:
          requests:
            memory: "1000Mi"
            cpu: "500m"
          limits:
            memory: "1500Mi"
            cpu: "1000m"
  
---

apiVersion: "autoscaling/v2"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "subscription"
  namespace: "app-deploy"
  labels:
    app: "subscription"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "subscription"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      target:
        type: "Utilization"
        averageUtilization: 75
